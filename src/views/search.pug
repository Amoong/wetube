extends base.pug
include mixins/video

block content
  form(method="GET").search__form
    input(placeholder="Search by title", name="keyword", type="text")
    button
      i.fas.fa-search

  each video in videos
    div.video-grid
      +video(video)
  else
    p.empty__message No videos found